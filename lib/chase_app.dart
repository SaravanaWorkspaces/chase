import 'package:flutter/material.dart';

class ChaseApp extends StatelessWidget {
  const ChaseApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("Hey You.."),
        ),
      ),
    );
  }
}
